
  
version: "3.9"

services:
  django:
    build: .
    container_name: app
    ports:
      - "8000:8000"
    volumes:
      - .:/app               
      - sqlite_data:/app/db  
    command: bash -c "python3 manage.py migrate && python manage.py runserver 0.0.0.0:8000"

volumes:
  sqlite_data:
